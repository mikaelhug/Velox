/* CVeloxNet is a header-only interop module for the velox-net Rust staticlib.
 * SwiftPM requires a C target to have at least one source file; this is it. The
 * actual implementation lives in libveloxnet.a (built by Scripts/build-net.sh)
 * and is linked into VeloxCore via linkerSettings. See include/veloxnet.h. */
